import Image from "next/image";import { ArrowUpLeft } from "lucide-react";import { ExperienceIcon } from "@/lib/icon-registry";import type { ExperienceItem } from "@/types/experience";
export function ItemCard({item,index,onClick}:{item:ExperienceItem;index:number;onClick:()=>void}){return <button className={`item-card${item.id==="clinics"?" item-card-featured":""}${item.visualType==="profile"?" item-card-profile":""}`} type="button" onClick={onClick} aria-label={`استكشف ${item.title}`}>
{item.visualType==="profile"?<span className="card-media card-profile-media"><span className="profile-orbit"/><b>{item.initials}</b><i><ExperienceIcon name="doctor"/></i></span>:<span className="card-media"><Image src={item.coverImage} alt="" fill sizes="(max-width: 1100px) 30vw, 24vw" style={{objectPosition:item.coverPosition??"center"}}/></span>}
<span className="card-shade"/><span className="card-number">{String(index).padStart(2,"0")}</span><span className="card-icon" aria-hidden><ExperienceIcon name={item.icon}/></span><span className="card-copy"><small>{item.subtitle}</small><strong>{item.title}</strong><span>{item.shortDescription}</span></span><span className="card-arrow"><ArrowUpLeft/></span></button>}
