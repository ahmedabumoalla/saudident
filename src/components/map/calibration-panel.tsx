"use client";
import { Copy, Minus, Plus } from "lucide-react";
import type { ExperienceItem } from "@/types/experience";
export function CalibrationPanel({ items, updateScale }: { items: ExperienceItem[]; updateScale: (id:string,delta:number)=>void }) { const json = JSON.stringify(items.map(({id,point,cameraTarget})=>({id,point,cameraTarget})),null,2); return <aside className="calibration-panel"><header><strong>معايرة العلامات</strong><button onClick={()=>navigator.clipboard.writeText(json)}><Copy size={18}/> نسخ JSON</button></header><div className="calibration-list">{items.map((item)=><div key={item.id}><span>{item.title}</span><code>{item.point?.x.toFixed(3)}, {item.point?.y.toFixed(3)}</code><button onClick={()=>updateScale(item.id,-.05)} aria-label="تقليل التكبير"><Minus size={16}/></button><b>{item.cameraTarget?.scale.toFixed(2)}</b><button onClick={()=>updateScale(item.id,.05)} aria-label="زيادة التكبير"><Plus size={16}/></button></div>)}</div><pre>{json}</pre></aside>; }
